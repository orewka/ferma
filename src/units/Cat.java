package farm.units;

public class Cat extends Pets {
    private int maxHealth = 8;

    public Cat(String name, int weight, int speed, int resources, int health) {
        super(name, weight, speed, resources, health);
    }
}
